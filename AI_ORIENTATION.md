# Ориентация для AI (ChatGPT) по проекту Constructor AI Platform

## 🎯 Обзор проекта

**Constructor AI Platform** - это многофункциональная платформа для создания брендов, сайтов и презентаций с помощью искусственного интеллекта. Проект находится в версии 3.8 и готов к использованию в продакшене.

## 📊 Ключевая информация

- **Версия**: 3.8
- **Статус**: Production Ready
- **Лицензия**: MIT
- **Основной язык**: TypeScript
- **Архитектура**: Монолитная с возможностью микросервисной (для разработки/продакшена)

## 🏗️ Технологический стек

### Backend
- **Runtime**: Node.js 18+
- **Framework**: Express.js
- **Язык**: TypeScript
- **База данных**: 
  - Текущая: Prisma с SQLite (dev.db)
  - Планируется: PostgreSQL
- **ORM**: Prisma
- **Валидация**: Zod
- **AI**: OpenAI API
- **WebSockets**: ws (для realtime синхронизации)

### Frontend
- **Framework**: React 19.2
- **Язык**: TypeScript
- **Сборщик**: Vite 7.2
- **Стилизация**: Tailwind CSS v4
- **Анимации**: Framer Motion, GSAP
- **3D**: Three.js, React Three Fiber
- **HTTP клиент**: Axios
- **Интернационализация**: i18next

### Инфраструктура
- **Контейнеризация**: Docker, Docker Compose
- **Веб-сервер**: Nginx
- **Мониторинг**: Prometheus, Grafana
- **CI/CD**: GitHub Actions (планируется)

## 📁 Структура проекта

```
constructor/
├── backend/                    # Backend сервер (монолитный)
│   ├── src/
│   │   ├── index.ts           # Точка входа Express сервера
│   │   ├── config/             # Конфигурация (AI, etc.)
│   │   ├── services/           # Бизнес-логика (100+ сервисов)
│   │   │   ├── projects.service.ts
│   │   │   ├── brand.service.ts
│   │   │   ├── ai-site-generator.service.ts
│   │   │   └── ... (множество других сервисов)
│   │   ├── middleware/         # Express middleware
│   │   │   ├── auth.middleware.ts
│   │   │   └── errorHandler.ts
│   │   ├── utils/              # Утилиты
│   │   │   ├── logger.ts
│   │   │   ├── prisma.ts
│   │   │   ├── metrics.ts
│   │   │   └── ...
│   │   ├── validation/         # Валидация схем (Zod)
│   │   ├── docs/               # Swagger документация
│   │   └── __tests__/          # Тесты
│   ├── prisma/
│   │   ├── schema.prisma       # Схема базы данных
│   │   └── migrations/         # Миграции БД
│   ├── grafana/                # Grafana дашборды
│   ├── scripts/                # Скрипты
│   └── package.json
│
├── frontend/                    # React приложение
│   ├── src/
│   │   ├── App.tsx             # Главный компонент
│   │   ├── main.tsx            # Точка входа
│   │   ├── components/         # React компоненты
│   │   │   ├── SiteBuilder/    # Конструктор сайтов
│   │   │   ├── PresentationBuilder/  # Конструктор презентаций
│   │   │   ├── GraphicsEditor/ # Редактор графики
│   │   │   └── ... (множество компонентов)
│   │   ├── services/           # API клиенты и сервисы
│   │   │   ├── api.ts          # Основной API клиент
│   │   │   ├── ai-site-generator.ts
│   │   │   ├── LogoGenerator.ts
│   │   │   └── ...
│   │   ├── utils/              # Утилиты
│   │   ├── hooks/              # React hooks
│   │   ├── constants/          # Константы
│   │   ├── workers/            # Web Workers
│   │   └── styles/             # Стили
│   ├── e2e/                    # E2E тесты (Playwright)
│   └── package.json
│
├── services/                    # Микросервисы (для продакшена)
│   ├── api-gateway/            # API Gateway
│   ├── auth-service/           # Сервис аутентификации
│   ├── projects-service/       # Сервис проектов
│   ├── ai-service/             # AI сервис
│   └── export-service/         # Сервис экспорта
│
├── mobile/                      # Мобильное приложение (React Native)
│   └── src/
│       ├── screens/
│       └── services/
│
├── desktop/                     # Desktop приложение (Electron)
│   └── main.js
│
├── docs/                        # Документация
│   ├── API.md                   # API документация
│   ├── DEPLOYMENT.md            # Развертывание
│   ├── DOCKER.md                # Docker руководство
│   ├── SECURITY.md              # Безопасность
│   ├── PERFORMANCE.md           # Производительность
│   └── plans/                   # Планы развития
│
├── scripts/                     # Скрипты автоматизации
│   ├── setup.sh / setup.bat
│   ├── quick-start.sh / quick-start.bat
│   └── publish-to-github.sh / publish-to-github.bat
│
├── docker-compose.yml           # Docker Compose конфигурация
├── nginx.conf                   # Nginx конфигурация
└── package.json                 # Корневой package.json
```

## ✨ Основные функции платформы

### 1. Создание брендов
- Генерация логотипов с помощью AI
- Создание цветовых палитр
- Подбор шрифтов
- Экспорт бренд-кита в ZIP

### 2. Конструктор сайтов
- Drag-and-Drop редактор
- 12+ типов блоков (text, image, gallery, testimonials, faq, etc.)
- AI-генерация контента
- SEO-оптимизация
- Локализация контента
- Экспорт в HTML

### 3. Редактор графики
- Magic Resize для соцсетей
- 20+ форматов (Instagram, Facebook, Twitter, LinkedIn, etc.)
- Генерация соц-постов с AI

### 4. Генератор презентаций
- Cinema Mode
- Экспорт в PDF
- Анимации и переходы

### 5. Дополнительные функции
- Система аутентификации и тарифов (Free, Pro, Brand Kit)
- Роли и коллаборация
- Маркетплейс шаблонов
- Аналитика сайтов
- Realtime синхронизация
- Семантический поиск
- AI-чат помощник

## 🔌 API Endpoints

### Основные эндпоинты:
- `GET /api/projects` - Получить все проекты
- `GET /api/projects/:id` - Получить проект по ID
- `POST /api/projects` - Создать новый проект
- `PUT /api/projects/:id` - Обновить проект
- `DELETE /api/projects/:id` - Удалить проект
- `POST /api/projects/:id/export` - Экспортировать сайт в HTML
- `POST /api/projects/:id/generate-content` - AI генерация контента
- `POST /api/projects/:id/translate` - Перевод проекта
- `GET /health` - Health check

### Swagger документация:
Доступна по адресу `/api-docs` (если настроена)

## 🗄️ База данных

### Схема Prisma (основные модели):
- **Project** - Проекты пользователей
- **User** - Пользователи
- **Brand** - Бренд-киты
- **Lead** - Лиды/контакты
- И другие модели (см. `backend/prisma/schema.prisma`)

### Миграции:
Все миграции находятся в `backend/prisma/migrations/`

## 🚀 Запуск проекта

### Быстрый старт:
```bash
npm run setup          # Автоматическая установка
npm run dev            # Запуск backend + frontend
```

### Отдельный запуск:
```bash
# Backend (порт 3001)
cd backend && npm run dev

# Frontend (порт 5173)
cd frontend && npm run dev
```

### Docker:
```bash
docker-compose up --build
```

## 🔧 Конфигурация

### Backend (.env):
- `PORT` - Порт сервера (по умолчанию 3001)
- `DATABASE_URL` - URL базы данных
- `OPENAI_API_KEY` - API ключ OpenAI
- `JWT_SECRET` - Секрет для JWT токенов
- И другие переменные (см. `backend/.env.example`)

### Frontend (.env):
- `VITE_API_URL` - URL backend API
- `VITE_OPENAI_API_KEY` - API ключ OpenAI (опционально)
- И другие переменные (см. `frontend/.env.example`)

## 📝 Важные файлы для понимания проекта

### Документация:
- `README.md` - Основная документация
- `ARCHITECTURE.md` - Архитектура проекта
- `PROJECT_SUMMARY.md` - Краткое описание проекта
- `docs/API.md` - API документация
- `docs/DEPLOYMENT.md` - Развертывание
- `CHANGELOG.md` - История изменений

### Код:
- `backend/src/index.ts` - Точка входа backend
- `frontend/src/App.tsx` - Главный компонент frontend
- `backend/prisma/schema.prisma` - Схема базы данных
- `backend/src/services/projects.service.ts` - Основной сервис проектов

## 🧪 Тестирование

### Backend:
```bash
cd backend
npm test              # Запуск тестов
npm run test:watch    # Watch mode
npm run test:coverage # С покрытием
```

### Frontend:
```bash
cd frontend
npm test              # Запуск тестов
```

### E2E:
```bash
cd frontend
npm run test:e2e      # E2E тесты (Playwright)
```

## 🔐 Безопасность

- Валидация данных через Zod
- JWT аутентификация
- CORS настройки
- Rate limiting
- Helmet для безопасности Express
- Санитизация пользовательского ввода

## 📈 Мониторинг

- Health checks на `/health`
- Prometheus метрики
- Grafana дашборды (в `backend/grafana/`)
- Логирование через `logger.ts`

## 🎨 Стилизация

- **Tailwind CSS v4** - основной фреймворк
- **Framer Motion** - анимации
- **GSAP** - продвинутые анимации
- **Three.js** - 3D графики
- Кастомные CSS модули для специальных случаев

## 🌐 Интернационализация

- Используется i18next
- Поддержка множества языков
- Автоопределение языка браузера

## 🔄 Workflow разработки

1. **Создание фичи**: Создать ветку от `main`
2. **Разработка**: Писать код, тесты
3. **Тестирование**: Запустить тесты
4. **Коммит**: Закоммитить изменения
5. **Пул-реквест**: Создать PR для ревью

## 📦 Зависимости

### Основные зависимости Backend:
- express, cors, helmet
- prisma, @prisma/client
- zod (валидация)
- openai (AI)
- ws (WebSockets)
- archiver, pdf-lib (экспорт)

### Основные зависимости Frontend:
- react, react-dom
- vite
- tailwindcss
- framer-motion, gsap
- axios
- three, @react-three/fiber
- i18next

## 🐛 Известные ограничения

- В текущей версии используется SQLite для разработки (для продакшена рекомендуется PostgreSQL)
- AI-генерация требует API ключи OpenAI
- Некоторые функции могут быть в стадии разработки

## 🎯 Приоритеты при работе с проектом

1. **Следовать существующей архитектуре** - проект имеет четкую структуру
2. **Использовать TypeScript** - весь код на TypeScript
3. **Валидация данных** - использовать Zod схемы
4. **Обработка ошибок** - использовать существующие утилиты
5. **Тестирование** - писать тесты для новой функциональности
6. **Документация** - обновлять документацию при изменениях

## 📞 Полезные команды

```bash
# Установка всех зависимостей
npm run install:all

# Очистка и переустановка
npm run clean:install

# Линтинг
npm run lint

# Сборка
npm run build

# Запуск в продакшене
npm start
```

## 🔗 Полезные ссылки

- Swagger документация: `/api-docs` (если настроена)
- Health check: `http://localhost:3001/health`
- Frontend: `http://localhost:5173`
- Backend API: `http://localhost:3001/api`

---

**Важно для AI**: При работе с проектом всегда проверяйте существующую структуру и следуйте установленным паттернам. Проект большой и сложный, поэтому важно понимать контекст перед внесением изменений.

